const APIcontrol = (function() {

    const clientId= '';
    const clientSecret='';

    const _getToken = async () -> {
        const result = await fetch(''), {
            method: 'POST',
            headers:{
                'Content-Type' : '',
                
            }
        }
    }
})();